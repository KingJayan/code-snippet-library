name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 */6 * * *"   
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -fsS -X GET https://snips0.vercel.app/api/health || exit 0
